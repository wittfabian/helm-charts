## @section Global parameters
global:
  ## @param global.imageRegistry Global Docker Image registry
  imageRegistry: ""
  ## @param global.imagePullSecrets Global Docker registry secret names as an array
  imagePullSecrets: []

## @section Common parameters
## @param nameOverride String to partially override keycloak.fullname
nameOverride: ""
## @param fullnameOverride String to fully override keycloak.fullname
fullnameOverride: ""
## @param namespaceOverride String to override the namespace for all resources
namespaceOverride: ""
## @param commonLabels Labels to add to all deployed objects
commonLabels: {}
## @param commonAnnotations Annotations to add to all deployed objects
commonAnnotations: {}

## @section Keycloak image configuration
image:
  ## @param image.registry Keycloak image registry
  registry: docker.io
  ## @param image.repository Keycloak image repository
  repository: keycloak/keycloak
  ## @param image.tag Keycloak image tag (immutable tags are recommended)
  tag: "26.6.3@sha256:9b0330756022422149aa6502eb2def8cd47c6e1b000c7c65cdb13e7c0133e992"
  ## @param image.imagePullPolicy Keycloak image pull policy
  imagePullPolicy: Always
  ## @param image.command Keycloak container command
  command: "/opt/keycloak/bin/kc.sh"

## @section Deployment configuration
## @param replicaCount Number of Keycloak replicas to deploy
replicaCount: 1

## @section Pod annotations and labels
## @param podAnnotations Map of annotations to add to the pods
podAnnotations: {}
## @param podLabels Map of labels to add to the pods
podLabels: {}

## @section Pod configuration
## @param shareProcessNamespace Enable process namespace sharing between containers in the pod
shareProcessNamespace: false

## @section extra volumes and volumes mount
## @param extraVolumes Optionally specify extra list of additional volumes for Keycloak pod
extraVolumes: []
## @param extraVolumeMounts Optionally specify extra list of additional volumeMounts for Keycloak container
extraVolumeMounts: []
## @param preserveThemes Disable mounting an emptyDir over /opt/keycloak/themes, preserving the themes bundled in the image.
preserveThemes: false
## @param preserveProviders Disable mounting an emptyDir over /opt/keycloak/providers, preserving the providers bundled in the image.
preserveProviders: false

## @section Extra init containers for Keycloak pod
## @param Optionnaly specify an extra list of initContainer for the Keycloak pod
## Example:
## initContainers:
##   - name: <container-name>
##     image: <your-image>
##     imagePullPolicy: Always
##
extraInitContainers: []

## @section Extra containers for Keycloak pod
## @param extraContainers Additional containers to add to the pod
## Example:
## extraContainers:
##   - name: sidecar
##     image: nginx:latest
##     imagePullPolicy: Always
##     ports:
##       - containerPort: 8000
##     volumeMounts:
##       - name: tmp
##         mountPath: /tmp
extraContainers: []

## @section Security Context
podSecurityContext:
  ## @param podSecurityContext.fsGroup Group ID for the volumes of the pod
  fsGroup: 1001
  ## @param podSecurityContext.seccompProfile Seccomp profile for the pod
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  ## @param containerSecurityContext.allowPrivilegeEscalation Enable container privilege escalation
  allowPrivilegeEscalation: false
  ## @param containerSecurityContext.runAsNonRoot Configure the container to run as a non-root user
  runAsNonRoot: true
  ## @param containerSecurityContext.runAsUser User ID for the Keycloak container
  runAsUser: 1001
  ## @param containerSecurityContext.runAsGroup Group ID for the Keycloak container
  runAsGroup: 1001
  ## @param containerSecurityContext.readOnlyRootFilesystem Mount container root filesystem as read-only
  readOnlyRootFilesystem: false
  ## @param containerSecurityContext.capabilities.drop Linux capabilities to be dropped
  capabilities:
    drop:
      - ALL

## @section Keycloak Configuration
keycloak:
  ## @param keycloak.adminUser Keycloak admin username
  adminUser: admin
  ## @param keycloak.adminPassword Keycloak admin password
  adminPassword: ""
  ## @param keycloak.existingSecret Name of existing secret to use for Keycloak admin credentials
  existingSecret: ""
  ## @param keycloak.secretKeys Secret keys for admin credentials
  secretKeys:
    adminPasswordKey: "admin-password"
  ## @param keycloak.hostname Keycloak hostname including scheme (e.g. https://keycloak.example.com)
  hostname: ""
  ## @param keycloak.hostnameAdmin Keycloak admin hostname including scheme (e.g. https://keycloak.example.com)
  hostnameAdmin: ""
  ## @param keycloak.hostnameStrict Enable strict hostname resolution
  hostnameStrict: false
  ## @param keycloak.hostnameBackchannel Keycloak backchannel hostname including scheme (e.g. https://keycloak.example.com)
  hostnameBackchannel: ""
  ## @param keycloak.httpEnabled Enable HTTP listener
  httpEnabled: true
  ## @param keycloak.httpsEnabled Enable HTTPS listener
  httpsEnabled: false
  ## @param keycloak.httpPort HTTP port
  httpPort: 8080
  ## @param keycloak.httpsPort HTTPS port
  httpsPort: 8443
  ## @param keycloak.managementPort Management interface port (health endpoints are served here)
  managementPort: 9000
  ## @param keycloak.proxyHeaders The proxy headers that should be accepted by the server. (forwarded, xforwarded). Must be set when running behind a reverse proxy (e.g. nginx ingress), otherwise Keycloak will not trust X-Forwarded-* headers and will generate incorrect URLs. Leave empty only when Keycloak is directly exposed without a proxy.
  proxyHeaders: ""
  ## @param keycloak.proxyProtocolEnabled  Whether the server should use the HA PROXY protocol when serving requests from behind a proxy. (true, false(default))
  proxyProtocolEnabled: false
  ## @param keycloak.proxyTrustedAddresses A comma separated list of trusted proxy addresses
  proxyTrustedAddresses: ""
  ## @param keycloak.production Enable production mode (runs 'start' instead of 'start-dev'). Should only be disabled for local development.
  production: true
  ## @param keycloak.httpRelativePath Set relative path for serving resources; must start with a /
  httpRelativePath: /
  ## @param keycloak.extraArgsPrefix Additional arguments to pass before the start command (e.g., for --config-file)
  extraArgsPrefix: []
  ## @param keycloak.extraArgs Additional arguments to pass to the Keycloak startup command
  extraArgs: []

## @section TLS Configuration
tls:
  ## @param tls.enabled Enable TLS/HTTPS support using custom certificates
  enabled: false
  ## @param tls.existingSecret Name of existing secret containing TLS certificate and key (PEM format)
  ## The secret should contain 'tls.crt' and 'tls.key' keys
  ## Note: If certManager.enabled is true, this will be auto-generated
  existingSecret: ""
  ## @param tls.certificateFile Path where the TLS certificate file will be mounted (internal)
  certificateFile: "/opt/keycloak/certs/tls.crt"
  ## @param tls.certificateKeyFile Path where the TLS certificate key file will be mounted (internal)
  certificateKeyFile: "/opt/keycloak/certs/tls.key"
  ## @section cert-manager Configuration
  ## @descriptionStart
  ## Configure cert-manager to automatically provision and renew TLS certificates
  ## Requires cert-manager to be installed in the cluster
  ## @descriptionEnd
  certManager:
    ## @param tls.certManager.enabled Enable cert-manager integration for automatic certificate provisioning
    enabled: false
    ## @param tls.certManager.issuerRef.name Name of the cert-manager Issuer or ClusterIssuer
    ## @param tls.certManager.issuerRef.kind Kind of the cert-manager issuer (Issuer or ClusterIssuer)
    ## @param tls.certManager.issuerRef.group Group of the cert-manager issuer (default: cert-manager.io)
    issuerRef:
      name: ""
      kind: ClusterIssuer
      group: cert-manager.io
    ## @param tls.certManager.duration Certificate duration (default: 2160h - 90 days)
    duration: ""
    ## @param tls.certManager.renewBefore Time before expiry to renew certificate (default: 360h - 15 days)
    renewBefore: ""
    ## @param tls.certManager.commonName Certificate common name (defaults to first dnsName if not specified)
    commonName: ""
    ## @param tls.certManager.dnsNames List of DNS names for the certificate (uses ingress.hosts if not specified)
    dnsNames: []
    ## @param tls.certManager.ipAddresses List of IP addresses for the certificate
    ipAddresses: []
    ## @param tls.certManager.secretName Name for the generated secret (defaults to <fullname>-tls)
    secretName: ""
    ## @param tls.certManager.usages Certificate key usages
    usages:
      - digital signature
      - key encipherment
    ## @param tls.certManager.annotations Additional annotations for the Certificate resource
    annotations: {}
  ## @param tls.truststoreEnabled Enable truststore for client certificate validation or outgoing HTTPS requests
  truststoreEnabled: false
  ## @param tls.truststoreExistingSecret Name of existing secret containing truststore file (Java Keystore format)
  ## The secret should contain 'truststore.jks' key or the key specified in the value 'tls.truststoreExistingSecretKey'
  truststoreExistingSecret: ""
  ## @param tls.truststoreExistingSecretKey Key within the truststore secret that holds the truststore file
  truststoreExistingSecretKey: "truststore.jks"
  ## @param tls.truststoreExistingSecretPasswordKey Key within the truststore secret that holds the truststore password
  truststoreExistingSecretPasswordKey: "truststore-password"
  ## @param tls.truststorePassword Password for the truststore (use with caution - consider using existingSecret)
  truststorePassword: ""
  ## @param tls.truststoreFile Path where the truststore file will be mounted (internal)
  truststoreFile: "/opt/keycloak/truststore/truststore.jks"

## @section Database Configuration
database:
  ## @param database.type Database type (postgres, mysql, mariadb, mssql, empty string)
  ## Note: If database.type is an empty string, all --db* settings will be omitted, e.g. type, url, etc.
  ## Note: H2 databases (h2-file, h2-mem) are not supported due to readonly filesystem restrictions
  ## Use embedded postgres or mariadb instead
  type: postgres
  ## @param database.host Database host (only used when not using embedded database)
  host: ""
  ## @param database.port Database port (only used when not using embedded database, defaults: postgres=5432, mysql/mariadb=3306, mssql=1443)
  port: ""
  ## @param database.schema Database schema
  schema: ""
  ## @param database.urlProperties Additional database url properties
  urlProperties: ""
  ## @param database.name Database name (only used when not using embedded database)
  name: keycloak
  ## @param database.username Database username (only used when not using embedded database)
  username: keycloak
  ## @param database.password Database password (only used when not using embedded database)
  password: ""
  ## @param database.existingSecret Name of existing secret for database credentials (only used when not using embedded database)
  existingSecret: ""
  ## @param database.secretKeys Secret keys for database credentials (only used when not using embedded database)
  secretKeys:
    passwordKey: "db-password"
    usernameKey: "db-username"
  ## @param database.jdbcParams Additional JDBC parameters
  jdbcParams: ""

## @section Cache Configuration
cache:
  ## @param cache.stack Cache stack (local, ispn, default)
  stack: local
  ## @param cache.configFile Custom cache configuration file
  configFile: ""

## @section Realm Configuration
realm:
  ## @param realm.import Enable import of realms from /opt/keycloak/data/import
  import: false
  ## @param realm.configFile Json config for initial realm configuration, mounted in /opt/keycloak/data/import
  configFile: ""
  ## @param realm.existingSecret Name of existing secret containing realm configuration (key: realm.json)
  existingSecret: ""

## @section Features Configuration
features:
  ## @param features.enabled List of enabled features
  enabled: []
  ## @param features.disabled List of disabled features
  disabled: []

## @section Service configuration
service:
  ## @param service.type Keycloak service type
  type: ClusterIP
  ## @param service.httpPort Keycloak HTTP service port
  httpPort: 8080
  ## @param service.httpsPort Keycloak HTTPS service port
  httpsPort: 8443
  ## @param service.httpTargetPort Keycloak HTTP container port
  httpTargetPort: 8080
  ## @param service.httpsTargetPort Keycloak HTTPS container port
  httpsTargetPort: 8443
  ## @param service.annotations Service annotations
  annotations: {}
  ## @param service.trafficDistribution Traffic distribution preference for the keycloak service. If the field is not set, the implementation will apply its default routing strategy.
  trafficDistribution: ""
  ## @param service.nodePort Keycloak HTTP node port
  httpNodePort: 30080
  ## @param service.nodePort Keycloak HTTPS node port
  httpsNodePort: 30443

## @section Ingress configuration
ingress:
  ## @param ingress.enabled Enable ingress record generation for Keycloak
  enabled: false
  ## @param ingress.className IngressClass that will be used to implement the Ingress
  className: ""
  ## @param ingress.annotations Additional annotations for the Ingress resource
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    # nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
  ## @param ingress.hosts[0].host Hostname for Keycloak ingress
  ## @param ingress.hosts[0].paths[0].path Path for Keycloak ingress
  ## @param ingress.hosts[0].paths[0].pathType Path type for Keycloak ingress
  hosts:
    - host: keycloak.local
      paths:
        - path: /
          pathType: Prefix
  ## @param ingress.tls TLS configuration for Keycloak ingress
  tls: []
  #  - secretName: keycloak-tls
  #    hosts:
  #      - keycloak.local

## @section Gateway API parameters
## TLS termination happens at the Gateway listener level, not at the backend service.
## To serve traffic from an HTTPS listener, set sectionName to the listener's name:
##
##   parentRefs:
##     - name: my-gateway
##       sectionName: https   # matches the HTTPS listener defined in the Gateway resource
##
## The backend always receives plain HTTP traffic regardless of the listener protocol.
gatewayAPI:
  httpRoute:
    ## @param gatewayAPI.httpRoute.enabled Enable Gateway API HTTPRoute generation for Keycloak
    enabled: false
    ## @param gatewayAPI.httpRoute.annotations Additional annotations for the HTTPRoute resource
    annotations: {}
    ## @param gatewayAPI.httpRoute.parentRefs References to the parent Gateways
    parentRefs:
      - name: gateway
        namespace: ""
        sectionName: ""
    ## @param gatewayAPI.httpRoute.hostnames List of hostnames to match
    hostnames:
      - keycloak.local
    ## @param gatewayAPI.httpRoute.rules HTTPRoute rules
    rules:
      - matches:
          - path:
              type: PathPrefix
              value: /

## @section Resources
resources: {}
  ## We usually recommend not to specify default resources and to leave this as a conscious
  ## choice for the user. This also increases chances charts run on environments with little
  ## resources, such as Minikube. If you do want to specify resources, uncomment the following
  ## lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 500m
  #   memory: 512Mi
  # requests:
  #   cpu: 250m
  #   memory: 256Mi

## @section Persistence
persistence:
  ## @param persistence.enabled Enable persistence using Persistent Volume Claims
  enabled: false
  ## @param persistence.storageClass Persistent Volume storage class
  storageClass: ""
  ## @param persistence.annotations Persistent Volume Claim annotations
  annotations: {}
  ## @param persistence.size Persistent Volume size
  size: 1Gi
  ## @param persistence.accessModes Persistent Volume access modes
  accessModes:
    - ReadWriteOnce
  ## @param persistence.existingClaim The name of an existing PVC to use for persistence
  existingClaim: ""

## @section Metrics Configuration
metrics:
  ## @param metrics.enabled Enable metrics endpoint on Keycloak
  enabled: false
  ## @section Metrics service parameters
  service:
    ## @param metrics.service.type Metrics service type
    type: ClusterIP
    ## @param metrics.service.port Metrics service port
    port: 9000
    ## @param metrics.service.targetPort Metrics service target port
    targetPort: http-metrics
    ## @param metrics.service.annotations Additional annotations for metrics service
    annotations: {}
    ## @param metrics.service.labels Additional labels for metrics service
    labels: {}
  ## @section ServiceMonitor parameters
  serviceMonitor:
    ## @param metrics.serviceMonitor.enabled Create ServiceMonitor resource for scraping metrics using PrometheusOperator
    enabled: false
    ## @param metrics.serviceMonitor.namespace Namespace in which ServiceMonitor is created
    namespace: ""
    ## @param metrics.serviceMonitor.interval Interval at which metrics should be scraped
    interval: 30s
    ## @param metrics.serviceMonitor.scrapeTimeout Timeout after which the scrape is ended
    scrapeTimeout: ""
    ## @param metrics.serviceMonitor.additionalLabels Additional labels for ServiceMonitor
    additionalLabels: {}
    ## @param metrics.serviceMonitor.annotations Additional annotations for ServiceMonitor
    annotations: {}
    ## @param metrics.serviceMonitor.relabelings RelabelConfigs to apply to samples before scraping
    relabelings: []
    ## @param metrics.serviceMonitor.metricRelabelings MetricRelabelConfigs to apply to samples before ingestion
    metricRelabelings: []
    ## @param metrics.serviceMonitor.honorLabels Specify honorLabels parameter to add the scrape endpoint
    honorLabels: false
    ## @param metrics.serviceMonitor.jobLabel The name of the label on the target service to use as the job name in prometheus
    jobLabel: ""

## @section Liveness and readiness probes
livenessProbe:
  ## @param livenessProbe.enabled Enable livenessProbe on Keycloak containers
  enabled: true
  ## @param livenessProbe.initialDelaySeconds Initial delay seconds for livenessProbe
  initialDelaySeconds: 60
  ## @param livenessProbe.periodSeconds Period seconds for livenessProbe
  periodSeconds: 30
  ## @param livenessProbe.timeoutSeconds Timeout seconds for livenessProbe
  timeoutSeconds: 5
  ## @param livenessProbe.failureThreshold Failure threshold for livenessProbe
  failureThreshold: 3
  ## @param livenessProbe.successThreshold Success threshold for livenessProbe
  successThreshold: 1

readinessProbe:
  ## @param readinessProbe.enabled Enable readinessProbe on Keycloak containers
  enabled: true
  ## @param readinessProbe.initialDelaySeconds Initial delay seconds for readinessProbe
  initialDelaySeconds: 30
  ## @param readinessProbe.periodSeconds Period seconds for readinessProbe
  periodSeconds: 10
  ## @param readinessProbe.timeoutSeconds Timeout seconds for readinessProbe
  timeoutSeconds: 5
  ## @param readinessProbe.failureThreshold Failure threshold for readinessProbe
  failureThreshold: 3
  ## @param readinessProbe.successThreshold Success threshold for readinessProbe
  successThreshold: 1

startupProbe:
  ## @param startupProbe.enabled Enable startupProbe on Keycloak containers
  enabled: true
  ## @param startupProbe.initialDelaySeconds Initial delay seconds for startupProbe
  initialDelaySeconds: 30
  ## @param startupProbe.periodSeconds Period seconds for startupProbe
  periodSeconds: 10
  ## @param startupProbe.timeoutSeconds Timeout seconds for startupProbe
  timeoutSeconds: 5
  ## @param startupProbe.failureThreshold Failure threshold for startupProbe
  failureThreshold: 60
  ## @param startupProbe.successThreshold Success threshold for startupProbe
  successThreshold: 1

## @section Node Selection
## @param nodeSelector Node labels for pod assignment
nodeSelector: {}

## @param priorityClassName for pod eviction
priorityClassName: ""

## @param tolerations Toleration labels for pod assignment
tolerations: []

## @param topologySpreadConstraints Topology Spread Constraints for pod assignment
topologySpreadConstraints: []

## @param affinity Affinity settings for pod assignment
affinity: {}

## @section Service Account
serviceAccount:
  ## @param serviceAccount.create Specifies whether a service account should be created
  create: false
  ## @param serviceAccount.annotations Annotations to add to the service account
  annotations: {}
  ## @param serviceAccount.name The name of the service account to use. If not set and create is true, a name is generated using the `fullname` template.
  name: ""
  ## @param serviceAccount.automountServiceAccountToken whether to automount the SA token inside the pod
  automountServiceAccountToken: false

## @param extraEnvVars Additional environment variables to set
extraEnvVars: []
  # - name: CUSTOM_VAR
  #   value: "custom-value"
  # - name: SECRET_VAR
  #   valueFrom:
  #     secretKeyRef:
  #       name: my-secret
  #       key: secret-key

## @param extraEnvVarsSecret Name of a secret containing additional environment variables
extraEnvVarsSecret: ""

## @param extraObjects Array of extra objects to deploy with the release
extraObjects: []

## @section Init Container Configuration
initContainers:
  copyQuarkusLib:
    ## @param initContainers.copyQuarkusLib.resources Resource requests and limits for the copy-quarkus-lib init container
    resources: {}
  waitForPostgres:
    ## @param initContainers.waitForPostgres.image PostgreSQL init container image for waiting (leave empty to use registry/repository/tag)
    image: ""
    ## @param initContainers.waitForPostgres.registry PostgreSQL image registry (overrides global.imageRegistry)
    registry: ""
    ## @param initContainers.waitForPostgres.repository PostgreSQL image repository
    repository: postgres
    ## @param initContainers.waitForPostgres.tag PostgreSQL image tag
    tag: "18.4@sha256:8ff36f3c66371cba71d20ceedccfc3de9669a68737607888c4ef0af93abe8e39"
    ## @param initContainers.waitForPostgres.pullPolicy PostgreSQL image pull policy
    pullPolicy: IfNotPresent
    ## @param initContainers.waitForPostgres.resources Resource requests and limits for PostgreSQL init container
    resources: {}
      # limits:
      #   cpu: 100m
      #   memory: 64Mi
      # requests:
      #   cpu: 100m
      #   memory: 32Mi
  waitForMariadb:
    ## @param initContainers.waitForMariadb.image MariaDB init container image for waiting (leave empty to use registry/repository/tag)
    image: ""
    ## @param initContainers.waitForMariadb.registry MariaDB image registry (overrides global.imageRegistry)
    registry: ""
    ## @param initContainers.waitForMariadb.repository MariaDB image repository
    repository: mariadb
    ## @param initContainers.waitForMariadb.tag MariaDB image tag
    tag: "12.3.2@sha256:b1c7bf836e64ed9406a8984af29509f40089d55cea14b32f12c4726a1f17104b"
    ## @param initContainers.waitForMariadb.pullPolicy MariaDB image pull policy
    pullPolicy: IfNotPresent
    ## @param initContainers.waitForMariadb.resources Resource requests and limits for MariaDB init container
    resources: {}
      # limits:
      #   cpu: 100m
      #   memory: 64Mi
      # requests:
      #   cpu: 100m
      #   memory: 32Mi

## @section PostgreSQL Configuration (when postgres.enabled=true)
## @descriptionStart
## PostgreSQL configuration for embedded database
## @descriptionEnd
postgres:
  ## @param postgres.enabled Enable embedded PostgreSQL database
  enabled: true
  ## @param postgres.auth.database PostgreSQL database name
  ## @param postgres.auth.username PostgreSQL database user (leave empty for default 'postgres')
  ## @param postgres.auth.password PostgreSQL database password
  ## @param postgres.auth.existingSecret Existing secret containing PostgreSQL credentials
  auth:
    database: "keycloak"
    username: ""
    password: ""
    existingSecret: ""

## @section MariaDB Configuration (when mariadb.enabled=true)
## @descriptionStart
## MariaDB configuration for embedded database
## @descriptionEnd
mariadb:
  ## @param mariadb.enabled Enable embedded MariaDB database (set to false when using postgres)
  enabled: false
  ## @param mariadb.auth.database MariaDB database name
  ## @param mariadb.auth.username MariaDB database user (leave empty for root user)
  ## @param mariadb.auth.password MariaDB database password
  ## @param mariadb.auth.rootPassword MariaDB root password
  auth:
    database: "keycloak"
    username: ""
    password: ""
    rootPassword: ""
    ## @param mariadb.auth.existingSecret Existing secret containing MariaDB root and user passwords
    existingSecret: ""
    ## @param mariadb.auth.secretKeys Secret keys for MariaDB credentials
    secretKeys:
      rootPasswordKey: mariadb-root-password
      userPasswordKey: mariadb-password
